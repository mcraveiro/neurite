#!/usr/bin/gnuplot

set title "Plot"
set xlabel "X"
set ylabel "Y"
# set ytics "10"
plot "crap.dat" title "" with lines
pause -1 "Hit any key to continue"

# plot "crap.dat" using 1:1 title 'Column', "crap.dat" using 1:2 title 'Beam' with lines
 cty# pause -1
